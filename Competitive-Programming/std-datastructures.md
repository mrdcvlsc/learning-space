# C++ STL Data Structure Notes

## Sets

- `std::set` - elements can not repeat.
- `std::multiset` - this is like a `std::set` but elements can repeat.
- commonly implemented with a red-black tree.
- the `lower_bound` and `upper_bound` are useful for non-overlapping interval allocation problems.